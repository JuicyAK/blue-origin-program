import React from 'react';
import ReactDOM from 'react-dom/client';
import BlueOriginProgram from './blue-origin-app';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BlueOriginProgram />
  </React.StrictMode>
);
